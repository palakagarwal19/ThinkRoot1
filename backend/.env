# NeuroLead Environment Configuration

# Google Gemini API key (optional – falls back to CSV templates if not set)
GEMINI_API_KEY=

# Apollo API key (optional – falls back to CSV data if not set)
APOLLO_API_KEY=

# Clay API key (optional – falls back to CSV data if not set)
CLAY_API_KEY=

# Absolute path to leads CSV
LEADS_CSV_PATH=C:\Users\Palak\OneDrive\Desktop\lead app\apollo-contacts-export-(2)-(1)-Default-view-export-1776574386695.csv

# Server config
HOST=0.0.0.0
PORT=8000
